# Инструкции за многоезична функционалност

## ✅ Какво е направено:

1. **Създаден е `translations.json`** - Съдържа всички преводи на български и английски
2. **Добавен е CSS** за language switcher бутона
3. **Добавен е JavaScript** в `main.js` за превключване на езиците
4. **Частично обновен е `index.html`** с data-i18n атрибути

## 📝 Как да редактираш преводите:

### Отвори `translations.json` и редактирай текстовете:

```json
{
  "bg": {
    "nav": {
      "home": "НАЧАЛО",  // Редактирай тук
      "machines": "КАНТОСЛЕПВАЩИ МАШИНИ"
    }
  },
  "en": {
    "nav": {
      "home": "HOME",  // Редактирай тук
      "machines": "EDGEBANDERS"
    }
  }
}
```

### Важни термини:
- **Кантослепваща машина** = **edgebander** (на английски)
- **Кантираща машина** = **edgebanding machine**
- **Кантослепване** = **edgebanding**

## 🔧 Как да добавиш превод на нов елемент:

### 1. Добави в HTML файла `data-i18n` атрибут:

```html
<!-- Преди -->
<h1>Производство на кантослепващи машини</h1>

<!-- След -->
<h1 data-i18n="hero.title1">Производство на кантослепващи машини</h1>
```

### 2. Добави превода в `translations.json`:

```json
{
  "bg": {
    "hero": {
      "title1": "Производство на кантослепващи машини"
    }
  },
  "en": {
    "hero": {
      "title1": "Manufacturing of Edgebanding Machines"
    }
  }
}
```

### 3. За HTML съдържание (с тагове):

```html
<p data-i18n="about.intro" data-i18n-html="true">
  Текст с <strong>удебелени</strong> думи
</p>
```

### 4. За placeholder на input полета:

```html
<input data-i18n="contact.name" placeholder="Вашето име">
```

## 🎨 Language Switcher бутон:

Добави това във всеки HTML файл след `</ul>` в навигацията, преди затварящия `</div>` на navbar-collapse:

```html
<!-- Language Switcher -->
<div class="language-switcher">
  <button class="lang-btn active" data-lang="bg">BG</button>
  <span class="lang-separator">/</span>
  <button class="lang-btn" data-lang="en">EN</button>
</div>
```

## 📄 Списък на файлове за обновяване:

### ✅ Готови:
- `translations.json` - Всички преводи
- `css/styles.css` - Стилове за бутона
- `js/main.js` - JavaScript функционалност
- `index.html` - **Частично** (навигация, hero, продукти, "За нас")

### ⚠️ Трябва да се добавят data-i18n атрибути:

**Страници:**
- `pages/kantoslepvashti-mashini.html`
- `pages/servizni-uslugi.html`
- `pages/vtora-upotreba.html`
- `pages/galeria.html`
- `pages/video-galeria.html`
- `pages/kontakti.html`

**Продукти:**
- `products/product-km7000.html`
- `products/product-km4001pe.html`
- `products/product-km900m.html`
- `products/product-km2015tpm.html`

## 🚀 Как да тестваш:

1. Отвори `index.html` в браузър
2. Кликни на бутона **EN** горе в navigation bar
3. Всички текстове с `data-i18n` атрибут ще се сменят на английски
4. Кликни **BG** за да върнеш на български
5. Езикът се запазва в localStorage

## 📋 Пример за завършване на index.html:

В секцията "Услуги":
```html
<h2 data-i18n="services.title">Услуги</h2>
<h3 data-i18n="services.service1">Ремонт с оригинални части</h3>
```

В секцията "Контакти":
```html
<h2 data-i18n="contact.title">Въпроси и поръчки</h2>
<label data-i18n="contact.name">Вашето име</label>
<input data-i18n="contact.name" placeholder="Вашето име">
<button data-i18n="contact.send">Изпрати</button>
```

## 🔄 За останалите страници:

1. Копирай language switcher кода от index.html
2. Добави `data-i18n` атрибути на текстовете
3. Провери дали пътят към translations.json е правилен:
   - От pages/: `../translations.json`
   - От products/: `../translations.json`
   - От root: `translations.json`

## 💡 Съвети:

- Използвай тачки (`.`) за вложени ключове: `"nav.home"`, `"about.title"`
- За HTML с тагове добави `data-i18n-html="true"`
- Бутонът запомня избрания език в localStorage
- Можеш да редактираш преводите директно в JSON файла
- За термини специфични за бизнеса, редактирай английските преводи

## 📞 Текущо покритие в translations.json:

- ✅ Навигация (nav)
- ✅ Hero секция
- ✅ Продукти
- ✅ За нас (about)
- ✅ Услуги (services)
- ✅ Контакти (contact)
- ✅ Footer
- ✅ Cookie banner
- ✅ Страници (machines_page, contact_page, product_page)

Преводите са професионални, но можеш да ги редактираш според специфичната терминология на бизнеса!
